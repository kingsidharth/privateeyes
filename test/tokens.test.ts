import { describe,it,expect } from 'vitest'; import { openDb } from '../src/db.js'; import { mintToken,verifyToken } from '../src/tokens.js';
describe('tokens',()=>{it('mints and verifies',()=>{const db=openDb(':memory:');const raw=mintToken(db,'test');expect(raw).toMatch(/^pe_/);expect(verifyToken(db,raw).token?.name).toBe('test');expect(verifyToken(db,'pe_bad').error).toBe('invalid_token');});it('detects revoke and expiry',()=>{const db=openDb(':memory:');const raw=mintToken(db,'x',-1);expect(verifyToken(db,raw).error).toBe('expired_token');const raw2=mintToken(db,'y');db.prepare('UPDATE tokens SET revoked=1').run();expect(verifyToken(db,raw2).error).toBe('revoked_token');});});
