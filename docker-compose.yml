services:
  privateeyes:
    build: .
    env_file: .env
    environment:
      BIND_HOST: 0.0.0.0
    ports:
      - "${BIND_HOST:-127.0.0.1}:8790:8790"
    volumes:
      - ./data:/data
    restart: unless-stopped
